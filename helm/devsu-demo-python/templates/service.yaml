apiVersion: v1
kind: Service
metadata:
  name: {{ include "devsu-demo-python.fullname" . }}
  labels:
    {{- include "devsu-demo-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "devsu-demo-python.selectorLabels" . | nindent 4 }}
